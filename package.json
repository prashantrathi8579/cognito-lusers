{
  "name": "cognito-lusers",
  "version": "1.0.0",
  "description": "CLI script to get a list of cognito users based on any attribute including custom.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Prashant Rathi",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.186.0",
    "commander": "^9.4.1",
    "fs": "0.0.1-security",
    "loglevel": "^1.8.0"
  }
}